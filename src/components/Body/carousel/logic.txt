

  {Stores &&
            Stores.map((item) => {
              return (
                <SwiperSlide key={item.id}>
                  <div className="HomeCarousel_form">
                    <img src={item.imgUrl} alt="" />
                    <p>{item.name}</p>
                    <p>{item.price}</p>
                  </div>
                </SwiperSlide>
              );
            })}